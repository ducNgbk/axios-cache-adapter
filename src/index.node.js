import { setup, setupCache, serializeQuery } from './api'
import RedisStore from './redis'
import IoRedisStore from './ioredis'
import RedisDefaultStore from './redis-default'

export { setup, setupCache, serializeQuery, RedisStore, IoRedisStore, RedisDefaultStore }
export default { setup, setupCache, serializeQuery, RedisStore, IoRedisStore, RedisDefaultStore }
